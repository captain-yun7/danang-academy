# 2026-04-22 — 프로젝트 초기 셋업

## 한 일
- 두 원본 PDF 분석
  - `0420 다프 MVP 상세 개발 문서 v1.pdf` (7p) — 목표 스펙
  - `0414다낭메뉴얼.pdf` (72p) — Glide 프로토타입 매뉴얼
- `_docs/` 구조 생성 및 스펙 문서 정제
  - `overview.md` — 제품 개요, 일정, 스택
  - `specs/0420-mvp-spec.md` — MVP 5개 기능 정제
  - `specs/0414-glide-prototype.md` — Glide 시트/로직 → Next 이식 매핑
  - `specs/db-schema.md` — Supabase 스키마 설계
  - `specs/api-contract.md` — 웹↔워커 DB 기반 메시지 패싱 계약
  - `runbook/init.sql` — 초기 스키마 SQL
  - `runbook/env.md` — 환경 변수 가이드
  - `runbook/architecture.md` — 배포 구조
- Next.js 16 + React 19 + TypeScript + Tailwind 4 + Turbopack 스캐폴딩
  - 레포 루트에 바로 배치 (서브디렉토리 → 루트로 승격)
  - `src/app/` App Router 구조
- git init (main 브랜치), `.gitignore` 병합 정리

## 결정
- **스택 확정**: Next.js 16 App Router + Supabase + Python 워커(맥미니) — MVP 문서의 권장안 그대로
- **프로토타입 이식 우선**: Glide 시트 스키마(`FreePronunciationRequests`)를 `free_pronunciation_tests`로 그대로 매핑
- **웹↔워커 결합은 DB 메시지 패싱**으로 (HTTP API 안 만들고 Supabase 테이블로 주고받음) — 복잡도 최소화
- **레이트 리밋은 서버/DB 레벨**로 강제 (Glide는 클라이언트 카운터였음)
- **상담 리드/테스트 데이터 분리**: 테스트 완료 시 자동으로 `consult_leads` 생성하지 않음 — 방문자가 상담 CTA 눌러야 생성 (의도 구분)

## 다음 할 일 (W1)
- [ ] Supabase 프로젝트 생성 (Vercel Marketplace 통합) + `init.sql` 실행
- [ ] `@supabase/ssr` 설치 및 클라이언트 헬퍼 작성
- [ ] 랜딩 페이지 `src/app/page.tsx` 초안
- [ ] 무료 발음 테스트 플로우 뼈대
  - [ ] `/free-pronunciation/start` (이름/레벨/모국어 입력 → 테스트 시작)
  - [ ] `/free-pronunciation/[id]/record` (녹음 UI)
  - [ ] `/free-pronunciation/[id]/result` (폴링 → 결과)
- [ ] 환경 변수 `.env.example` 생성
- [ ] README 덮어쓰기 (프로젝트 설명)
